{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main Takit window. Plugins are driven from Rust, so the frontend only needs core IPC + events.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
